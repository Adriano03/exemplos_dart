void main() {
  // Com o const não é possível alterar a lista, adicionar ou retirar nomes;
  final List lista = const ['Adriano', 'Katrine', 'Daiane'];
  // lista.add('Josué');
  print(lista);
  // Se for final não é possivél atribuir nada a mesma váriável;
  // lista = ['a', 'a'];
  // print(lista);
}
